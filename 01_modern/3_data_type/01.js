// 데이터 타입의 종류
// 2가지로 나뉨
// (1) 원시 타입(Primitive Types)
// - string, number, boolean, undefined, null
// (2) 참조 타입(복잡 / Reference Types)

/** 원시타입 */
// 정수
let age = 25;
let count = 100;
let negative = -10;

// 실수
let price = 19.99;
let pi = 3.14159;

// 특수한
let infinity = Infinity;
let negInfinity = -Infinity;
let notANumber = NaN; // 직접 여러분이 지정할일은 없음

let sum = 10 + 20;
